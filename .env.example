# ===================================================================
# CORE APPLICATION CONFIGURATION
# ===================================================================

# --- Core Application Security ---
# IMPORTANT: Use 'openssl rand -hex 32' in your terminal to generate strong, unique keys for production.
SECRET_KEY="your-strong-flask-secret-key"
JWT_SECRET_KEY="your-strong-jwt-secret-key"

# --- CORS Configuration ---
# Comma-separated list of allowed frontend origins.
# Use "*" for development only, but specify domains for production.
ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# --- Demo User Credentials ---
# For demonstration purposes. Replace with a real authentication system in production.
DEMO_USER="admin"
DEMO_PASSWORD="your-secure-demo-password"

# --- JWT Token Lifetime ---
# The lifetime of the access token, specified in hours.
JWT_ACCESS_TOKEN_EXPIRES_HOURS=1 

# --- Database ---
# Connection string for the PostgreSQL database.
DATABASE_URL="postgresql://user:password@host:port/database_name"

# --- Rate Limiting Cache ---
# Connection URI for Memcached. The default assumes a Docker Compose setup.
MEMCACHED_URL="memcached://memcached:11211"


# ===================================================================
# AI MODEL & SERVICE CONFIGURATION
# ===================================================================

# --- Google Cloud & Vertex AI ---
# These variables are required for the Gemini agent and RAG retrieval.
GOOGLE_APPLICATION_CREDENTIALS="/app/google-credentials.json"
GOOGLE_CLOUD_PROJECT="your-gcp-project-id"
GOOGLE_CLOUD_LOCATION="us-central1"
GENAI_MODEL_NAME="gemini-1.5-flash-001" 
RAG_CORPUS="projects/your-gcp-project-id/locations/us-central1/ragCorpora/your-corpus-id"

# --- DeepSeek API ---
# Required for the DeepSeek agent.
DEEPSEEK_API_KEY="your-deepseek-api-key"
DEEPSEEK_MODEL_NAME="deepseek/DeepSeek-V2"

# --- OpenAI API ---
# Required for the OpenAI agent.
OPENAI_API_KEY="your-openai-api-key"
OPENAI_MODEL_NAME="openai/gpt-4o"


# App Name
COMPOSE_PROJECT_NAME=chatbot-app